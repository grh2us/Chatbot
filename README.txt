Download the whole folder first
Open a terminal and cd into folder
Run npm install
Run npm run dev
Open a browser at localhost:3000
Enter a message and then answer the proceeding questions
Close the browser
All the entered information will have been stored as a collection in a mongoDB

Notes/ To-Do's:
Change the questions to match those asked in healthcare form / Taking care of validation
Upon completion redirect user to a new page that shows completed form
Front End Changes For Better UI